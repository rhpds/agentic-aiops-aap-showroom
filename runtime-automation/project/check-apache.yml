---
# Stub: Check Apache httpd status on node1
- name: Check Apache httpd service status
  hosts: node1
  become: true
  tasks:
    - name: Get httpd service status
      ansible.builtin.systemd:
        name: httpd
      register: httpd_status

    - name: Print httpd status
      ansible.builtin.debug:
        msg: "httpd is {{ httpd_status.status.ActiveState }}"
